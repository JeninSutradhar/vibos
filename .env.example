NEXT_PUBLIC_CALENDLY_URL=https://calendly.com/suhail-kiarainfo/30min?hide_landing_page=1&hide_gdpr_banner=1

# PHP API (optional). If set, lead forms + newsletter + analytics hit this host (/api/leads/*, etc.).
# If unset, contact/quote/callback forms use built-in Next routes (/api/contact, /api/quote, /api/callback).
# Local PHP: http://localhost:8000  |  Production: https://api.vibosglobal.com (https when the site is https)
NEXT_PUBLIC_PHP_API_BASE_URL=http://localhost:8000

DATABASE_URL=postgres://user:password@localhost:5432/vibos

SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
LEADS_NOTIFY_TO=leads@yourcompany.com
LEADS_NOTIFY_FROM=website@yourcompany.com
